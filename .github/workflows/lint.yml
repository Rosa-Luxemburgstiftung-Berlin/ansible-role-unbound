---
name: ansible-lint
on:
  pull_request:
  push:
  schedule:
    - cron: "25 6 * * 1"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          path: 'rls.unbound'
      - name: run ansible-lint
        uses: ansible/ansible-lint-action@f92921ffd076d017d88ad248626a6d3bf0aa1dbb
        with:
          override-deps: |
            rich>=9.5.1,<11.0.0
            ansible==5.3.0
            ansible-lint==5.3.1
          targets: "./"
          args: "-q"
